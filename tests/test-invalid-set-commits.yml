name: Test invalid set_commits value error
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      SENTRY_ORG: my-org
      SENTRY_AUTH_TOKEN: fake-token-for-test
      MOCK: "true"
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run action with invalid set_commits value (expect failure)
        id: action
        uses: .
        with:
          release: test-release-1.0.0
          projects: my-project
          finalize: "false"
          set_commits: invalid-value
          disable_telemetry: "true"
        continue-on-error: true

      - name: Assert action failed with invalid set_commits error
        run: |
          echo "outcome: ${{ steps.action.outcome }}"
          if [ "${{ steps.action.outcome }}" != "failure" ]; then
            echo "FAIL: expected action to fail when set_commits has invalid value"
            exit 1
          fi
          echo "PASS: action correctly failed with invalid set_commits value"
